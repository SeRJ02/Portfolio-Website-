---
interface Props {
  title: string;
  sub?: string;
}
const { title, sub } = Astro.props;
---
<header class="page-header">
  <h1>{title}</h1>
  {sub && <p class="page-header__sub">{sub}</p>}
</header>
